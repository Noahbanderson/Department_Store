<h1><%= @dep.id ? "Edit Department Name" : "Create New Department" %></h1>
<hr />
<%= form_for @dep do |f| %>
  <%= f.label :dep_name %>
  <%= f.text_field :dep_name, required: true, autofocus: true %>
  <%= f.submit  %>
<% end %>